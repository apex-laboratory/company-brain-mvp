async def extract_skill(content: str, source_id: str) -> dict:
    """Two-stage ExIde pipeline: raw text → pseudo-code → condition-action-dependency triple."""
    raise NotImplementedError("Phase 3")
