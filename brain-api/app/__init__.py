"""Brainite backend (convention-compliant module).

This package follows BACKEND_BEST_PRACTICES.md: a thin ``main`` app factory,
self-contained feature modules under ``modules/``, and cross-cutting concerns
under ``shared/``. See the best-practices doc for the layering rules.
"""
