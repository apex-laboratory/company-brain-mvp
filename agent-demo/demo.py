"""
Phase 6 agent demo — Company Brain end-to-end scenario.

Scenario: Customer requesting refund, 38 days post-purchase, $200 order,
claims item arrived damaged in transit.

The agent calls query_brain via FastMCP, receives the matched skill with
graph_context showing DamagedInTransit OVERRIDES TimeWindowRule, and
applies the override to produce a grounded resolution.
"""


def main():
    print("Phase 6 demo — not yet implemented.")
    print("Run after Phase 5 delivery interface is complete.")


if __name__ == "__main__":
    main()
